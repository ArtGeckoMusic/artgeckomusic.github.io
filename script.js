// const Xebra = require("./xebra");

$(document).ready(function() {
  let webSocket = new WebSocket("ws://127.0.0.1");
  // var xebraState;
  // var options = {
  //   hostname: "127.0.0.1",
  //   port: 80,
  //   supported_objects: ["button"]
  // };
  // xebraState = new Xebra.State(options);
});